@Library("jenkinsgl@master") _

pipeline {
    agent {
        any
    }
    stages {
        stage('Initialize') {
            steps {
                script {
                   echo "Hello"
                }
            }
        }
    }
}
